<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.furnature.mapper.BoardMapper">
	
	<!--게시판 조회-->
	<select id="selectBoardList" parameterType="hashmap" resultType="com.example.furnature.model.Board">
		SELECT *
		FROM TBL_BOARD
	</select>
	
	<!--게시글 등록-->
	<insert id="insertBoard" parameterType="hashmap">
		INSERT INTO TBL_BOARD(BOARD_NO, BOARD_TITLE, BOARD_CONTENTS, USER_ID, CDATETIME, UDATETIME) 
		VALUES(BOARD_SEQ.NEXTVAL, #{title}, #{contents}, 'test1', SYSDATE, SYSDATE)
	</insert>
</mapper>
